# Keyboard-Keycode-Detector
Keyboard Keycode Detector ⌨️  An interactive web project built using HTML, CSS, and JavaScript that detects and displays keyboard key values, key codes, and event codes in real time whenever a user presses a key. The project demonstrates DOM manipulation, keyboard events, dynamic UI updates, and responsive card-based layouts.
